Drop the proving artifacts here:

  transaction.wasm           (circuit witness generator)
  transaction_final.zkey     (Groth16 proving key)

These are shipped alongside the Veilo zk-circuits project. See the
root README.md ("Proving keys") for exact copy instructions.

They are NOT committed to git because the zkey is ~100MB.
